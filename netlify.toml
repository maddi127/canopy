[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client-side routes (/diy/*) resolve on direct load / refresh
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
